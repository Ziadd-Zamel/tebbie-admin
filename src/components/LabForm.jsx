import React from 'react'

const LabForm = ({ initialData, mode = "add", isLoading, error }) => {
    const token = localStorage.getItem("authToken");

  return (
    <section>
      
    </section>
  )
}

export default LabForm
